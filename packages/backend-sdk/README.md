# Cryptly Backend SDK

Type-safe frontend client generated from Cryptly's Nest OpenAPI schema.

The SDK treats encrypted payloads as opaque strings. It must not log plaintext secrets, passphrases, private keys, or unwrapped project keys.
