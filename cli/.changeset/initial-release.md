---
"@cryptly/cli": minor
---

First release of `@cryptly/cli`. Authorize a device against your cryptly
account, link a directory to a project, and pull/push end-to-end encrypted
secrets between the project and a local file.
